apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "telemetry-generator.fullname" . }}-test-connection"
  labels:
    {{- include "telemetry-generator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "telemetry-generator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
